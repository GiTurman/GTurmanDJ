import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.jsx' // დარწმუნდით, რომ App.jsx ფაილი არსებობს

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)